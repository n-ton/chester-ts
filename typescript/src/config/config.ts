import { capsConfig } from "./caps.config";

export const config = {
    baseUrl: 'provide url',
    waitUntil: 10000,
    timeouts: {
        script: 10000,
        pageLoad: 10000,
        implicit: 10000
    }
}